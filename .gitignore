# --- Xcode / build artifacts (all regenerated by `xcodegen generate`) ---
Journal.xcodeproj/
Journal/Info.plist
DerivedData/
build/
*.xcresult
*.xcuserstate
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# --- Swift Package Manager ---
.build/
.swiftpm/
Package.resolved

# --- Snapshot test failures (reference images ARE committed under __Snapshots__) ---
**/__Snapshots__/**/*.failure.png
FailureDiffs/

# --- macOS ---
.DS_Store

# --- Secrets — NEVER commit (Claude API key lives here in later stages) ---
secrets.xcconfig
*.env
.env

# --- Local planning / research docs (kept out of the public repo by request) ---
docs/
UI_Interface/
master-plan.md
PLAN.md
