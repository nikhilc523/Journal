# --- Xcode / build artifacts (all regenerated by `xcodegen generate`) ---
Sediment.xcodeproj/
Sediment/Info.plist
DerivedData/
build/
*.xcresult
*.xcuserstate
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# --- Swift Package Manager ---
.build/
.swiftpm/
Package.resolved

# --- Snapshot test failures (reference images ARE committed under __Snapshots__) ---
**/__Snapshots__/**/*.failure.png
FailureDiffs/

# --- macOS ---
.DS_Store

# --- Secrets — NEVER commit (Claude API key lives here in later stages) ---
secrets.xcconfig
*.env
.env

# --- Local planning / research docs (kept out of the public repo by request) ---
docs/
UI_Interface/
master-plan.md
PLAN.md

# --- Freddy avatar: AGPL-licensed engine source (studio export). Kept local so
# the copyleft engine code stays out of the remote; the native Swift port +
# bundled `Sediment/Resources/freddy.avatar.json` (parametric design data) are
# what ship. See memory/freddy-mascot.md for the licensing note. ---
freddy-avatar/
